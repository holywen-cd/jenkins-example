pipeline {
    agent any

    stages {
        stage ('Trigger Stage') {

            steps {
                sh " echo $GIT_BRANCH is the current branch"
                sh ' echo Triggering CD'
            }
        }
    }
}
